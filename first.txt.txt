#include<stdio.h>

int (int n)

{
	int s=1;
	printf("Enter the n value:");
	scanf("%d",&int n);
	
	if (n==s)
	{
		printf("True");
	}
	else
	{
		printf("False");
	}
	return 0;
}
void main()

{
	int p;
	printf("Enter the p value:");
	scanf("%d",&p);
	
	if (p>4)
	{
		printf("True");
	}
	else
	{
		printf("False");
	}
}
//edited by bhanu
/*
#include <stdio.h>
#define MAX_SIZE 100

void main()
{
	int m, n, a[MAX_SIZE][MAX_SIZE], i, j;

	printf("Enter the size of rows :");
	scanf("%d", &m);

	printf("Enter the size of columns :");
	scanf("%d", &n);

	printf("Enter the elements :");
	for (i = 0; i < m; i++)
	{
		for (j = 0; j < n; j++)
		{
			scanf("%d", &a[i][j]);
		}
	}

	printf("The elements are :");
	for (i = 0; i < m; i++)
	{
		for (j = 0; j < n; j++)
		{
			printf("%d", a[i][j]);
		}
		printf("\n");
	}
*/
void main()
{
	int A,B,C;
	A=10;
	B=20
	C=A+B;
	printf("C=%d",c);
}
